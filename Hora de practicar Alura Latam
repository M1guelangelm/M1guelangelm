alert("¡Bienvenida y bienvenido a nuestro sitio web!");

// Declaración de variables
let nombre = "Lua";
let edad = 25;
let numeroDeVentas = 50; // Sin espacios en el nombre de la variable
let saldoDisponible = 1000;

// Mensajes de alerta
alert("¡Error! Completa todos los campos");
let mensajeDeError = "¡Error! Completa todos los campos";
alert(mensajeDeError);

// Solicitar datos al usuario
nombre = prompt("Por favor, deje acá su nombre:");
edad = prompt("Por favor, deje acá su edad:");

// Convertir la edad a número para compararla
edad = Number(edad);

// Verificar si puede obtener su licencia de conducir
if (edad >= 18) {
    alert("¡Puedes obtener tu licencia de conducir!");
} else {
    alert("Lo sentimos, aún no puedes obtener tu licencia de conducir.");
}
